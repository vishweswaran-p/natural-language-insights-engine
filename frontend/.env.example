# Optional. The app calls the backend at a same-origin "/api" by default, which
# works when the backend serves the built frontend and via the Vite dev proxy.
# Override only if the API lives on a different origin.
# VITE_API_BASE_URL=http://localhost:1337/api
